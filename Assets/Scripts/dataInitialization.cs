using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/// <summary>
/// 外部ファイル読み込みのための初期化処理。
/// もし、データファイルが存在しない場合には、データファイルを作成する.
/// 作成している場合は、読み込みのチェックだけする。
/// ファイルが規定の状態でない場合にはエラーコードを出して、もう一度初期化する。
/// </summary>
public class dataInitialization : MonoBehaviour
{

    private void Awake()
    {
        
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
